export type TasksInputModel = {
  title: string;
  description: string;
  responsible: string;
  concluded: boolean;
}

export type TasksViewInputModel = {
  title: string;
  description: string;
  responsible: string;
  concluded: boolean;
}